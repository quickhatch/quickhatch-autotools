# Makefile.am

aclocaldir = $(datadir)/aclocal
quickhatch = quickhatch.m4

# static data/scripts (require explicit distribution)
static_data = $(quickhatch)

dist_aclocal_DATA = $(static_data)

EXTRA_DIST = gnulib-cache.m4
